[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[project]
name = "artenic-ai-sdk-ensemble"
version = "0.7.0"
description = "Ensemble strategies for the Artenic AI SDK"
readme = "../../../README.md"
license = "Apache-2.0"
requires-python = ">=3.12"
authors = [
    { name = "Artenic Cloud", email = "dev@artenic.cloud" },
]

dependencies = [
    "artenic-ai-sdk>=0.7.0",
]

[project.optional-dependencies]
torch = ["torch>=2.0"]

[tool.hatch.build.targets.wheel]
packages = ["src/artenic_ai_sdk_ensemble"]
