[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[project]
name = "artenic-ai-cli"
version = "0.6.0"
description = "Artenic AI — Command-line interface"
readme = "../../README.md"
license = "Apache-2.0"
requires-python = ">=3.12"
authors = [
    { name = "Artenic Cloud", email = "dev@artenic.cloud" },
]

dependencies = [
    "artenic-ai-sdk",
    "click>=8.0",
    "httpx>=0.27",
    "rich>=13.0",
]

[project.scripts]
artenic = "artenic_ai_cli.main:cli"

[tool.hatch.build.targets.wheel]
packages = ["src/artenic_ai_cli"]
